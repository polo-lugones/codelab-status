# Robtec CodeLab — página de estado

Página pública de estado de [codelab.robtec.dev](https://codelab.robtec.dev).
`status.json` lo actualiza cada 30 minutos un workflow del repo principal
(checks de plataforma, API y base de datos desde GitHub Actions — infraestructura
independiente del hosting).
